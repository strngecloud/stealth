# Settings Feature

Mailbox policies, account preferences, notification rules, key management, and network settings.

For the mailbox **policy editing** surface (who can reach a user's inbox) — the
standalone `/policy-editor` route, the Settings "Inbox control" tab, and the
shared `mailbox-policy-templates.ts` contract — see the contributor handoff:
[`src/routes/policy-editor/POLICY_EDITOR.md`](../../routes/policy-editor/POLICY_EDITOR.md).
